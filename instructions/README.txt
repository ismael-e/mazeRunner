
1 Place the maze text files to be processed in the src/mazeFiles directory.
  (The directory already has some sample files)

2 Run the application via the Main class.
  (The application will list out all the maze files in the mazeFiles directory)

3 Choose the maze to be solved from the list by typing its index via the console and hit enter.